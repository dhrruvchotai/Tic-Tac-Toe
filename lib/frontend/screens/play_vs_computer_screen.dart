import 'package:flutter/material.dart';

class PlayVsComputerScreen extends StatelessWidget {
  const PlayVsComputerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("Play vs Computer")),
    );
  }
}
